# Research workspace

Cloned upstream projects for engine evaluation. **Clones are gitignored** — re-clone with:

```bash
./research/scripts/clone.sh
```

Tracked artifacts:

| File | Purpose |
|------|---------|
| `SPIKE.md` | Engine comparison, decisions, gaps |
| `scripts/clone.sh` | Reproducible clone list |
| `fixtures/` | Optional local OCR test images (not committed large binaries) |

See `SPIKE.md` for findings and default engine choice.
